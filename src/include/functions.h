#include <cstdint>

#ifndef FUNCTIONS_H
#define FUNCTIONS_H

namespace func {
    bool is_identity_1_to_n_pandigital(int multiplicand, int multiplier, int n);
    int64_t product_sum(int n);
}

#endif